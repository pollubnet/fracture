@using Fracture.Server.Modules.MapGenerator.Models.Map
<div class="node-info">
    <span>Biome: @Node?.Biome.Name</span>
    <span>Location: @(string.IsNullOrWhiteSpace(Node?.GroupName)
                            ? Node.LocationType
                            : Node.GroupName)</span>
</div>

@code {
    [Parameter] public Node? Node { get; set; }

}